<?xml version='1.0' encoding='UTF-8'?><!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN' 'conf/dtd/mybatis-3-mapper.dtd'><mapper namespace='Template'>
   <select id='getTemplate'
           parameterType='java.util.Map'
           resultType='java.lang.String'>
		SELECT TEMPLATE
		FROM TEMPLATE
		WHERE GROUP_ID = #{groupId} AND ID = #{id}
	</select>
   <insert id='insertTemplate' parameterType='java.util.Map'>
		INSERT INTO
		TEMPLATE (GROUP_ID, ID, TEMPLATE)
		VALUES (#{groupId}, #{id}, #{template})
	</insert>
   <update id='updateTemplate' parameterType='java.util.Map'>
		UPDATE TEMPLATE
		SET TEMPLATE = #{template}
		WHERE GROUP_ID = #{groupId} AND ID = #{id}
	</update>
   <delete id='deleteTemplate' parameterType='java.util.Map'>
		DELETE FROM TEMPLATE
		<where>
         <if test='groupId != null'>AND GROUP_ID = #{groupId}</if>
      </where>
	  </delete>
</mapper>

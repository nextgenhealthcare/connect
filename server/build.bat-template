set SERVER_PATH="YOUR SERVER PATH"
set CLIENT_PATH="YOUR CLIENT PATH"
set MANAGER_PATH="YOUR MANAGER PATH"
set VERSION=1.6.1
set SCHEMA_VERSION=1

cd %SERVER_PATH%

call ant -buildfile mirth-build.xml create-setup -Dserver=%SERVER_PATH% -Dclient=%CLIENT_PATH% -Dmanager=%MANAGER_PATH% -Dversion=%VERSION% -DschemaVersion=%SCHEMA_VERSION%

pause
name=mirth-core-client-base
version=4.6.0

src=${basedir}/src
test=${basedir}/test
lib=${basedir}/lib
classes=${basedir}/classes
test_classes=${basedir}/test_classes
dist=${basedir}/dist

core.models=../core-models
core.util=../core-util
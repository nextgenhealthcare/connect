<project name="mirth-client" basedir="." default="help">
	<target name="help">
		<echo>Mirth Client Build Help</echo>
		<echo>-----------------------</echo>
	</target>

	<target name="init">
		<property file="build.properties" />

		<path id="classpath">
			<fileset dir="${lib}" includes="*.jar" />
		</path>

		<mkdir dir="${logs}" />

		<property name="extensions" value="${dist}/extensions" />

		<!-- connectors -->
		<property name="connectors.doc" value="${extensions}/com.webreach.mirth.connectors.doc" />
		<property name="connectors.email" value="${extensions}/com.webreach.mirth.connectors.email" />
		<property name="connectors.file" value="${extensions}/com.webreach.mirth.connectors.file" />
		<property name="connectors.http" value="${extensions}/com.webreach.mirth.connectors.http" />
		<property name="connectors.dicom" value="${extensions}/com.webreach.mirth.connectors.dicom" />
		<property name="connectors.jdbc" value="${extensions}/com.webreach.mirth.connectors.jdbc" />
		<property name="connectors.jms" value="${extensions}/com.webreach.mirth.connectors.jms" />
		<property name="connectors.js" value="${extensions}/com.webreach.mirth.connectors.js" />
		<property name="connectors.mllp" value="${extensions}/com.webreach.mirth.connectors.mllp" />
		<property name="connectors.soap" value="${extensions}/com.webreach.mirth.connectors.soap" />
		<property name="connectors.tcp" value="${extensions}/com.webreach.mirth.connectors.tcp" />
		<property name="connectors.vm" value="${extensions}/com.webreach.mirth.connectors.vm" />

		<!-- plugins -->
		<property name="plugins.dashboardstatus" value="${extensions}/com.webreach.mirth.plugins.dashboardstatus" />
		<property name="plugins.dicomviewer" value="${extensions}/com.webreach.mirth.plugins.dicomviewer" />
		<property name="plugins.extensionmanager" value="${extensions}/com.webreach.mirth.plugins.extensionmanager" />
		<property name="plugins.imageviewer" value="${extensions}/com.webreach.mirth.plugins.imageviewer" />
		<property name="plugins.javascriptrule" value="${extensions}/com.webreach.mirth.plugins.javascriptrule" />
		<property name="plugins.javascriptstep" value="${extensions}/com.webreach.mirth.plugins.javascriptstep" />
		<property name="plugins.mapper" value="${extensions}/com.webreach.mirth.plugins.mapper" />
		<property name="plugins.messagebuilder" value="${extensions}/com.webreach.mirth.plugins.messagebuilder" />
		<property name="plugins.messagepruner" value="${extensions}/com.webreach.mirth.plugins.messagepruner" />
		<property name="plugins.pdfviewer" value="${extensions}/com.webreach.mirth.plugins.pdfviewer" />
		<property name="plugins.rtfviewer" value="${extensions}/com.webreach.mirth.plugins.rtfviewer" />
		<property name="plugins.rulebuilder" value="${extensions}/com.webreach.mirth.plugins.rulebuilder" />
		<property name="plugins.serverlog" value="${extensions}/com.webreach.mirth.plugins.serverlog" />
		<property name="plugins.scriptfilestep" value="${extensions}/com.webreach.mirth.plugins.scriptfilestep" />
	</target>

	<target name="clean" depends="init">
		<delete dir="${classes}" />
		<delete dir="${dist}" />
	</target>

	<target name="compile" depends="clean, init">
		<mkdir dir="${classes}" />
		<javac srcdir="${src}" destdir="${classes}" debug="${javac.debug}" verbose="${javac.verbose}" deprecation="${javac.deprecation}" optimize="${javac.optimize}">
			<classpath refid="classpath" />
		</javac>

		<copy todir="${classes}" file="${src}/log4j.properties" />

		<!-- copy the image files so they will be included in jar -->
		<copy todir="${classes}/com/webreach/mirth/client/ui/images">
			<fileset dir="${src}/com/webreach/mirth/client/ui/images" />
		</copy>
	</target>

	<target name="create-client" depends="compile">
		<mkdir dir="${dist}" />
		<mkdir dir="${dist.source}" />

		<jar destfile="${dist}/${client.jar}">
			<fileset dir="${classes}">
				<exclude name="com/webreach/mirth/connectors/**/**" />
			</fileset>
			<fileset dir="${classes}">
				<include name="com/webreach/mirth/connectors/ConnectorClass.class" />
			</fileset>
		</jar>

		<!-- connectors -->
		<mkdir dir="${connectors.doc}" />
		<jar destfile="${connectors.doc}/doc-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/doc/**" />
		</jar>

		<mkdir dir="${connectors.email}" />
		<jar destfile="${connectors.email}/email-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/email/**" />
		</jar>

		<mkdir dir="${connectors.file}" />
		<jar destfile="${connectors.file}/file-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/file/**" />
		</jar>

		<mkdir dir="${connectors.http}" />
		<jar destfile="${connectors.http}/http-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/http/**" />
		</jar>

		<mkdir dir="${connectors.dicom}" />
		<jar destfile="${connectors.dicom}/dicom-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/dimse/**" />
		</jar>

		<mkdir dir="${connectors.jdbc}" />
		<jar destfile="${connectors.jdbc}/jdbc-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/jdbc/**" />
		</jar>

		<mkdir dir="${connectors.jms}" />
		<jar destfile="${connectors.jms}/jms-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/jms/**" />
		</jar>

		<mkdir dir="${connectors.js}" />
		<jar destfile="${connectors.js}/js-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/js/**" />
		</jar>

		<mkdir dir="${connectors.mllp}" />
		<jar destfile="${connectors.mllp}/mllp-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/mllp/**" />
		</jar>

		<mkdir dir="${connectors.soap}" />
		<jar destfile="${connectors.soap}/soap-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/soap/**" />
		</jar>

		<mkdir dir="${connectors.tcp}" />
		<jar destfile="${connectors.tcp}/tcp-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/tcp/**" />
		</jar>

		<mkdir dir="${connectors.vm}" />
		<jar destfile="${connectors.vm}/vm-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/connectors/vm/**" />
		</jar>

		<!-- plguins -->
		<mkdir dir="${plugins.messagepruner}" />
		<jar destfile="${plugins.messagepruner}/messagepruner-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/messagepruner/**" />
		</jar>

		<mkdir dir="${plugins.extensionmanager}" />
		<jar destfile="${plugins.extensionmanager}/extensionmanager-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/extensionmanager/**" />
		</jar>

		<mkdir dir="${plugins.javascriptstep}" />
		<jar destfile="${plugins.javascriptstep}/javascriptstep-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/javascriptstep/**" />
		</jar>

		<mkdir dir="${plugins.mapper}" />
		<jar destfile="${plugins.mapper}/mapper-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/mapper/**" />
		</jar>

		<mkdir dir="${plugins.messagebuilder}" />
		<jar destfile="${plugins.messagebuilder}/messagebuilder-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/messagebuilder/**" />
		</jar>
		<jar destfile="${dist}/plugins/externalscript-transformer-step-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/scriptfile/step/**" />
		</jar>

		<mkdir dir="${plugins.javascriptrule}" />
		<jar destfile="${plugins.javascriptrule}/javascript-filter-rule-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/javascriptrule/**" />
		</jar>

		<mkdir dir="${plugins.rulebuilder}" />
		<jar destfile="${plugins.rulebuilder}/rulebuilder-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/rulebuilder/**" />
		</jar>

		<mkdir dir="${plugins.dashboardstatus}" />
		<jar destfile="${plugins.dashboardstatus}/dashboardstatus-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/dashboardstatus/**" />
		</jar>

		<mkdir dir="${plugins.dicomviewer}" />
		<jar destfile="${plugins.dicomviewer}/dicomviewer-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/dicomviewer/**" />
		</jar>

		<mkdir dir="${plugins.pdfviewer}" />
		<jar destfile="${plugins.pdfviewer}/pdfviewer-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/pdfviewer/**" />
		</jar>

		<mkdir dir="${plugins.rtfviewer}" />
		<jar destfile="${plugins.rtfviewer}/rtfviewer-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/rtfviewer/**" />
		</jar>

		<mkdir dir="${plugins.imageviewer}" />
		<jar destfile="${plugins.imageviewer}/imageviewer-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/imageviewer/**" />
		</jar>

		<mkdir dir="${plugins.serverlog}" />
		<jar destfile="${plugins.serverlog}/serverlog-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/serverlog/**" />
		</jar>

		<mkdir dir="${plugins.scriptfilestep}" />
		<jar destfile="${plugins.scriptfilestep}/scriptfilestep-client.jar" basedir="${classes}">
			<include name="com/webreach/mirth/plugins/scriptfilestep/**" />
		</jar>

		<!-- copy source files -->
		<copy todir="${dist.source}">
			<fileset dir="${src}">
				<exclude name="**/*.form" />
			</fileset>
		</copy>

		<!-- remove svn folders -->
		<delete>
			<fileset dir="${dist.source}" includes="**/.svn" />
		</delete>

		<zip basedir="${dist.source}" destfile="${dist}/mirth-client-src.zip" />
		<delete dir="${dist.source}" />
	</target>
</project>